using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

public class StageMain : MonoBehaviour
{
    void Awake()
    {
        //Xml_ReWriter RWxml = new Xml_ReWriter();
        //Xml_ReWriter.stUserInfo info = new Xml_ReWriter.stUserInfo(1, 1);
        //RWxml.xmlWriteFile("MyInfo.xml", info);

        //stUserInfo info = new stUserInfo();
        //XmlRe_Writer RWxml = new XmlRe_Writer();
        //RWxml.xmlReadFile("MyInfo.xml", out info);
    }

    void Start()
    {
        
    }

    void Update()
    {
        
    }
}
